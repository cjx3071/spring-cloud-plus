spring:
  profiles:
    active: local
  application:
    name: sub

server:
  port: 10002
  servlet:
    encoding:
      enabled: true
      force: true
      charset: UTF-8
  # 开启优雅停机
  shutdown: graceful
# 日志
logging:
  file:
    path: ./logs
  level:
    root: info
